export const enum EnumToken {
  accessToken = "accessToken", // 30분
  refreshToken = "refreshToken", // 한달
}

export const enum EnumAuth {
  super = "SUPER",
  admin = "ADMIN",
}
export enum ViewBoard {
  wait = "wait",
  receipt = "receipt",
  complete = "complete",
}
